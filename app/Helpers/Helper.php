<?php // namespace App\Helpers;

// WE CAN REMOVE IT LATER

// use Form;

// class Helper {

//     public static function delete($routeParams, $label = 'Delete')
//     {
//         $form = Form::open(['method'=>'DELETE', 'action'=>$routeParams]);

//         $form .= Form::submit($label, ['class'=>'ui fluid red button', 'onclick'=>"if(!confirm('Son kararin?')){return false;};"]);

//         return $form .= Form::close();
//     }

// }